# Vendored Nerd Fonts

Self-hosted webfonts served at `/fonts/*` by the mobitty server.
Loaded by `packages/client/src/fonts.ts` and the inline preload
script in `packages/client/index.html`.

Run `scripts/vendor-fonts.sh` to regenerate (overwrites this directory).

Licenses: see `licenses/LICENSES.md`.
